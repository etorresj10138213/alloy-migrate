name: CI/CD: Deploy Grafana Alloy to Istio/GKE

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GKE_CLUSTER: ${{ secrets.GKE_CLUSTER }}
  GKE_ZONE: ${{ secrets.GKE_ZONE }}
  NAMESPACE: istio-system
  PROMETHEUS_CONFIGMAP_NAME: prometheus-config
  CONFIG_KEY: prometheus.yml

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 1. Autenticación en Google Cloud
      - id: 'auth'
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          
      # 2. Configuración de gcloud
      - name: Configurar gcloud y kubectl
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}
          setup_gcloud: true 

      # 3. Obtener Credenciales de GKE
      - name: Obtener credenciales del cluster GKE
        uses: google-github-actions/get-gke-credentials@v2
        with:
          cluster_name: ${{ env.GKE_CLUSTER }}
          location: ${{ env.GKE_ZONE }}

      - name: ⚙️ Install Grafana Alloy Binaries (ZIP)
        id: install_alloy
        run: |
          ALLOY_VERSION="v1.11.3"
          FILENAME="alloy-linux-amd64.zip"
          ALLOY_URL="https://github.com/grafana/alloy/releases/download/${ALLOY_VERSION}/${FILENAME}"
          
          echo "Intentando descargar Grafana Alloy ${ALLOY_VERSION}..."
          curl -sL --fail -o ${FILENAME} ${ALLOY_URL}
          
          if [ $? -ne 0 ]; then
            echo "Error: Fallo al descargar el archivo desde la URL. Verifica la versión y la URL."
            exit 1
          fi
          
          echo "Instalando unzip..."
          sudo apt-get update && sudo apt-get install -y unzip
          
          echo "Descomprimiendo el binario..."
          unzip ${FILENAME}
          
          if [ -f alloy-linux-amd64 ]; then
            sudo mv alloy-linux-amd64 /usr/local/bin/alloy
            chmod 777 /usr/local/bin/alloy
          else
            echo "Error: No se encontró el binario 'alloy' desempaquetado. Verifique el contenido del ZIP."
            exit 1
          fi
          alloy --version
          
          
      - name: 📥 Extract Prometheus Config from GKE ConfigMap
        id: extract
        run: |
          echo "Extrayendo configuración de Prometheus de ConfigMap: ${PROMETHEUS_CONFIGMAP_NAME}..."
          kubectl get configmap ${PROMETHEUS_CONFIGMAP_NAME} -n ${NAMESPACE} -o jsonpath="{.data.${CONFIG_KEY}}" > prometheus.yaml
          
          if [ ! -s prometheus.yaml ]; then
            echo "Error: El archivo prometheus.yaml está vacío. El ConfigMap o la clave son incorrectos."
            exit 1
          fi
          echo "Configuración de Prometheus extraída con éxito."
          
      - name: 🔄 Convert Prometheus to Alloy & Validate
        id: convert_validate # <-- ID del paso
        run: |
          echo "Realizando conversión a config.alloy"
          alloy convert --source-format=prometheus --output=config.alloy prometheus.yaml          
          
      - name: 🧪 Simulate Alloy Run (Dry Run)
        # CORRECCIÓN DE SINTAXIS/LÓGICA: Referencia al ID correcto 'convert_validate'
        if: success() && steps.convert_validate.outcome == 'success'
        run: |
          echo "Simulando ejecución de Alloy (Dry Run)..."
          alloy run config.alloy --dry-run
          echo "Simulación de ejecución exitosa."
      
      # 7. Despliegue a GKE 🚀
      - name: 🚀 Apply Alloy Configuration and Deployment
        run: |
          echo "Desplegando Grafana Alloy en el namespace ${NAMESPACE}..."
          
          # CORRECCIÓN DE SINTAXIS/LÓGICA: La ruta del archivo es 'config.alloy' (en la raíz)
          # 1. Crear/Actualizar ConfigMap
          kubectl create configmap grafana-alloy-config -n ${NAMESPACE} --from-file=config.alloy=config.alloy --dry-run=client -o yaml | kubectl apply -f -
          
          # 2. Aplicar el Deployment (que montará el ConfigMap)
          kubectl apply -f k8s/alloy-deployment.yaml
          
          echo "Despliegue de Grafana Alloy completado. Ahora actualiza la fuente de datos en Grafana (istio-system)."